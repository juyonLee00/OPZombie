using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bed : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //코루틴 사용해서 10초간 피로도 3씩 회복.
        //잠들 때 zzz표시 뜨는 UI 추가.
    }
}
